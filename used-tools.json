{
  "$id": "tag:quadriga-dk.de,2025:schema:used-tools",
  "description": "Liste von Tools, die bei der Erstellung des Buchs verwendet wurden.",
  "type": "array",
  "minItems": 1,
  "items": {
    "oneOf": [{
        "type": "string",
        "format": "uri"
      },
      {
        "description": "Strukturierte Angabe des Tools",
        "@id": "schema:softwareApplication",
        "properties": {
          "name": {
            "$ref": "schema.json#/$defs/multilingual-text"
          },
          "url": {
            "type": "string",
            "format": "uri"
          }
        }
      }
    ]
  }
}
